[tool.poetry]
name = "service-a"
version = "0.1.0"
description = "Sub-package A: HTTP service layer"
authors = ["mend-detection-qa"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.32.4"
httpx = "^0.27"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
